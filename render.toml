# Railway/Render deployment configuration
[build]
  command = "julia --project=. -e 'using Pkg; Pkg.instantiate()'"

[start]
  command = "julia --project=. -e 'include(\"src/LabelImgJL.jl\"); LabelImgJL.start(parse(Int, get(ENV, \"PORT\", \"8080\"))))'"
